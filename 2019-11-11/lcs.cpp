#include <iostream>
#include <string>


int main(int argc, char *argv[])
{
    size_t t, l1, l2;
    std::string s1, s2;

    std::cin >> t;

    for (size_t i = 0; i < t; i++) {
        std::cin >> l1 >> l2;
        std::cin >> s1 >> s2;
    }

    return 0;
}

